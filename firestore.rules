rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Custom Auth (localStorage) 기반 환경이므로 Firestore Client SDK의 모든 읽기/쓰기/삭제 권한을 허용합니다.
      allow read, write: if true;
    }
  }
}
