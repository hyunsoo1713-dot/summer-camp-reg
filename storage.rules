rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 행사 이미지 경로: 누구나 읽기/쓰기 허용 (인증 없이 이미지 업로드 가능)
    match /events/{eventId}/images/{imageFile} {
      allow read: if true;
      allow write: if true;
    }
  }
}
